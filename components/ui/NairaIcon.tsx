import { Banknote } from 'lucide-react';

/** Naira currency icon for metric cards */
export function NairaIcon({ className }: { className?: string }) {
  return <Banknote className={className} aria-hidden />;
}
